{
  "name": "figma-ai-wireframer-plugin",
  "version": "1.0.0",
  "description": "Figma plugin for AI-powered wireframe generation",
  "scripts": {
    "dev": "esbuild code.ts --bundle --outfile=code.js --global-name=figmaPlugin --target=es2020 --format=iife --sourcemap --watch",
    "build": "esbuild code.ts --bundle --outfile=code.js --global-name=figmaPlugin --target=es2020 --format=iife --minify"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.92.0",
    "esbuild": "^0.19.0"
  }
}